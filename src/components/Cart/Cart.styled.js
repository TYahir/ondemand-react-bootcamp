import styled from "styled-components";

export const StyledCart = styled.div`
    margin: 0;
    padding: 0;

    button {
        border: 0 none;
        outline: 0 none;
        background: transparent;
        cursor: pointer;

        svg {
           font-size: 2em;
        }
    }
`;