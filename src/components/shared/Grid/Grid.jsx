import { StyledGrid } from "./Grid.styled";

function Grid({ children }) {
    return ( 
        <StyledGrid>
            {children}
        </StyledGrid>
     );
}

export default Grid;<div>Grid</div>